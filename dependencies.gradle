ext {
    // Sdk
    minSdkVersionValue = '21'
    targetSdkVersionValue = '30'

    // Core
    gradleVersion = '4.2.0-beta06'
    kotlinVersion = '1.4.31'
    googlePlayServicesVersion = '4.3.2'
    fabricIoToolsVersion = '1.31.0'
    crashlyticsGradleVersion = '2.5.1'
    safeArgsGradleVersion = '1.0.0'

    // Base
    appcompatVersion = '1.2.0'
    legacySupportVersion = '1.0.0'
    constraintLayoutVersion = '2.0.4'
    materialVersion = '1.4.0-alpha01'
    androidCoreVersion = '1.5.0-beta03'
    androidFragmentVersion = '1.3.2'

    // Android's Architecture Components
    architectureComponentsVersion = '2.3.0'
    lifecycleExtensionsVersion = '2.2.0'

    // Databinding Library
    databindingLibraryVersion = '3.2.0-alpha10'

    // Coroutines
    coroutinesVersion = '1.3.0'

    // Firebase Libraries
    firebaseCoreVersion = '18.0.2'
    firebaseCrashlyticsVersion = '17.4.0'
    firebaseAnalyticsVersion = ''
    firebaseAuthVersion = '20.0.3'

    // Google Play Services Auth
    playServicesAuthVersion = '19.0.0'

    // Retrofit
    retrofitVersion = '2.9.0'
    retrofitFlowVersion = '0.2.0'

    // OkHTTP Logging interceptor
    okhttpLoggingInterceptorVersion = '5.0.0-alpha.2'

    // Testing
    testRunnerVersion = '1.2.0'
    testEspressoCoreVersion = '3.2.0'
    junitVersion = '4.12'

    // Glide
    glideVersion = '4.12.0'

    // CircleImageView
    circleImageViewVersion = '3.1.0'
    
    // Kodein
    kodeinVersion = '7.4.0'
    kodeinFrameworkVersion = '5.2.0'

    // Navigation component
    navComponentVersion = '2.3.4'
}