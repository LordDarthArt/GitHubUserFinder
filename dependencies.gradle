ext {
    // Sdk
    min_sdk_version = '21'
    target_sdk_version = '29'

    // Core
    gradle_version = '4.2.0-beta06'
    kotlin_version = '1.4.31'
    google_play_services_version = "4.3.2"
    fabric_io_tools_version = "1.31.0"

    // Base
    appcompat_version = '1.2.0'
    legacy_support_version = "1.0.0"
    constraint_layout_version = '2.0.4'
    materialVersion = '1.4.0-alpha01'
    androidCoreVersion = '1.5.0-beta03'
    androidFragmentVersion = '1.3.2'

    // Android's Architecture Components
    architectureComponentsVersion = '2.3.0'
    lifecycleExtensionsVersion = '2.2.0'

    // Databinding Library
    databinding_library_version = "3.2.0-alpha10"

    // Coroutines
    coroutinesVersion = "1.3.0"

    // Firebase Libraries
    firebase_core_version = '18.0.2'
    firebase_crashlytics_version = "2.10.1"
    firebase_auth_version = '20.0.3'

    // Google Play Services Auth
    play_services_auth_version = '19.0.0'

    // Retrofit
    retrofit_version = '2.9.0'

    // Dagger2
    dagger2_version = '2.25.2'

    // OkHTTP Logging interceptor
    okhttp_logging_interceptor_version = '5.0.0-alpha.2'

    // Testing
    test_runner_version = "1.2.0"
    test_espresso_core_version = "3.2.0"
    junit_version = "4.12"

    // Glide
    glide_version = '3.7.0'

    // Picasso
    picasso_version = '2.71828'

    // CircleImageView
    circle_image_view_version = '3.1.0'
}